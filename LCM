#include<iostream>

using namespace std;


int gcd(int a,int b)
{
            if(a==0)
	 return b;
	 
	gcd(b%a,a);
}


int main()
{
	int lcm=(12*60)/gcd(12,60);           //we know num1*num2=lcm*gcd
	cout<<lcm<<endl;
}
